spring:
  application:
    name: inventario
  datasource:
    url: jdbc:oracle:thin:@onlyfields_high?TNS_ADMIN=C:/Users/W608-PCXX/IdeaProjects/Wallet_ONLYFIELDS
    username: OF_INVENTARIO
    password: OnlyFields2026
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect
  flyway:
    enabled: true
    baseline-on-migrate: false
    locations: classpath:db/migration
  docker:
    compose:
      enabled: false

server:
  port: 8085
