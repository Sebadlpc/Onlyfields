server:
  port: 8084

spring:
  datasource:
    url: jdbc:mysql://db-mysql:3306/onlyfields_suscripciones?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html
    operationsSorter: alpha # Personalización 1: Ordenar operaciones alfabéticamente
    tagsSorter: alpha       # Personalización 2: Ordenar tags alfabéticamente
