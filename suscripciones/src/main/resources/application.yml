spring:
  application:
    name: suscripciones
  datasource:
    url: jdbc:oracle:thin:@onlyfields_high?TNS_ADMIN=C:/Users/seses/Desktop/GIT/Wallet_ONLYFIELDS
    username: OF_SUSCRIPCIONES
    password: OnlyFields2026
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect
  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 0
    locations: classpath:db/migration
  docker:
    compose:
      enabled: false

server:
  port: 8084